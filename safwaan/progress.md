# Safwaan — Progress

## Current Phase
**Phase 16 — Intervals (started 2026-07-07). 3/6 curriculum done (LC 57, LC 56, LC 252) + 2 bonus (LC 986, LC 3169). Phase 13 — Graphs still open (6/15 curriculum problems done). Phase 9 — Trees still open (5/15 curriculum problems done, next: LC 572). Picked up a Phase 3 (Sliding Window) bonus off-curriculum: LC 28. Also opened Phase 5 — Binary Search off-curriculum: LC 704 (curriculum #1) + 3 bonus (LC 35, LC 69, LC 34). Also opened Phase 4 — Stack: built a 5-problem stack fundamentals module (reverseSomeChars, pairedParentheses, befittingBrackets, decompressBraces, nestingScore), 3 bonus real LC problems (LC 345, LC 394, LC 856), and 3/7 curriculum done (LC 20, LC 155, LC 150). Picked up a Phase 1 (Arrays & Hashing) bonus off-curriculum: LC 349 and LC 2215, working through "missing hashing problems."**
Six graphs problems done across 2026-07-05 to 2026-07-06 (LC 200, 130, 997, 133, 399, 695) — the biggest single push so far.
Phase 2 complete as of 2026-06-09 (LC #42 done)
Returning from ~1 week break (SQL + interview prep). Interview was 2026-06-19.
⚠️ LC 42 two-pointer redo still deferred
⚠️ **Revisit queue is severely overdue (oldest: LC 3, due 2026-06-18) — SIXTEEN straight sessions deferred as of 2026-07-15.** Three different framings have failed, and the direct-conversation ask itself has now been offered twice (2026-07-14 second session, and implicitly again — this 2026-07-15 session opened straight into a new bonus problem with no revisit mention at all, not even a deferred offer). **Next session: this must be the literal first message, before reading any pasted code.** Consider a fourth, different tactic entirely — not another verbal framing, since three have already failed and two direct-conversation offers have gone unanswered. E.g., ask him to just pick ONE problem from the queue himself, no framing, no precondition, just "which one."
Next up: Meeting Rooms II (LC 253, Heap — new pattern) or Non-Overlapping Intervals (LC 435, Greedy) to continue Phase 16. Revisit queue is the top open issue overall, not just a per-session blocker.

For full curriculum and problem status → [TRACKER.md](../TRACKER.md)

---

## Recently Completed
- Find the Difference of Two Arrays (LC #2215, bonus) — 2026-07-16 — Off-curriculum Phase 1 (Arrays & Hashing) bonus, Easy, Hash Set — Cross-Set Membership (Difference variant), second problem of the hashing sweep same day as LC 349. Self-solved, correct on arrival — mirror-image of LC 349's intersection shape (Sets to kill duplicates, walk one Set checking non-membership in the other, in both directions). Accepted 202/202, 7ms runtime (88.49th percentile). Clean own-words explanation given unprompted, correctly named the duplicate-removal purpose of the Sets and the two-direction membership-check structure. Notably: no implicit-global slip this time — both result arrays correctly declared with `const`, unlike the LC 349/977/167 recurring bug (patterns.md #10). Single data point, not enough to close that open question yet.
- Intersection of Two Arrays (LC #349, bonus) — 2026-07-16 — Off-curriculum Phase 1 (Arrays & Hashing) bonus, Easy, Hash Set — Cross-Set Membership. Self-solved, brought a fully correct working solution (convert both arrays to Sets, iterate one checking membership in the other), accepted 57/57, 1ms runtime (78.72nd percentile). One recurring minor bug — `result = []` with no `const`/`let`, an implicit global, same shape as the LC 977 `right` bug (patterns.md #10) — worked by accident, not self-caught before submission. Correctly reasoned that the Set-based dedup is only needed *because* of the duplicate-elements constraint; without it, a simpler single-pass membership check would suffice — good constraint-sensitivity instinct, unprompted. Confirmed the solution is already time/space optimal (O(n+m), can't do better) when asked "is there a better way," and was shown the ES2024 native `Set.prototype.intersection` as a modern alternative (informational, not required).
- Evaluate Reverse Polish Notation (LC #150) — 2026-07-15 — Phase 4 curriculum #3, Postfix Expression Evaluation via Stack. Near one-shot solve — correctly self-identified the core RPN mechanism (push operands, on an operator pop the two most recent as right/left operand in the correct order, apply, push result back) without hints. One real bug: used `Math.floor` for integer division, which rounds toward negative infinity rather than truncating toward zero — fails on cases like `7 / -3` (floor gives `-3`, problem expects `-2`). Guided through a concrete trace (`Math.floor(-2.333)` → `-3`, wrong) and a wrong-turn attempt at `Math.round` (rounds to nearest, not toward zero — also wrong in general, e.g. `2.75`) before asking directly for the fix; corrected to `Math.trunc`. Self-attributed the near-one-shot speed to prior exposure ("saw this years back... built the formulas for [RPN] anyways"), not overconfidence — appropriately honest self-assessment. Accepted after the fix.
- Min Stack (LC #155) — 2026-07-15 — Phase 4 curriculum #2, Auxiliary Min-Tracking Stack — first genuinely new stack shape today, no fundamentals-module mapping. **Video-assisted (NeetCode), disclosed honestly upfront.** Brought a working two-parallel-stack solution with one real bug — `pop()` referenced an undefined `value` variable (leftover from `push`'s parameter, not declared in `pop`'s scope) — self-caught it in one guided question ("what does `value` refer to inside `pop`?"). Accepted after the fix, 41.11th percentile runtime, 42.18th percentile memory. Then engaged genuinely with a follow-up Socratic discussion on the space-optimized variant (only push to `minStack` when a new minimum is set, pop from it conditionally) — correctly self-derived the core mechanism ("if the popped value equals the top of minStack, that means it's the current minimum, so pop it from minStack too") with zero direct answers given, though he explicitly deferred writing the implementation to a future session. Notably different from prior video-assisted sessions (LC 200, LC 3169): real debugging happened AND the follow-up conceptual extension was actively reasoned through, not declined.
- Score of Parentheses (LC #856, bonus) — 2026-07-15 — Off-curriculum Phase 4 (Stack) bonus, Medium difficulty, Score Accumulation via Stack. Fourth confirmed instance in the same day of a course-fundamentals pattern transferring cold to a real LeetCode problem — unprompted connected this to `nestingScore` from the fundamentals module, and this time it's an exact match, not a generalization: same seed-stack-with-0, push-0-on-open, pop-and-fold-on-close (+1 if direct match, ×2 if nested) shape, no changes needed. Clean, correct, no bugs, no hints. Declined the live own-words explanation again ("you can explain it") — per his own stated preference (2026-07-15, LC 394 discussion), this is mood-dependent and not pushed further; explanation written directly into the learnings file.
- Decode String (LC #394, bonus) — 2026-07-15 — Off-curriculum Phase 4 (Stack) bonus, Medium difficulty, Nested Group Decompression. Third confirmed instance in the same day of a course-fundamentals pattern transferring cold to a real LeetCode problem — unprompted connected this to `decompressBraces` from the fundamentals module (pop-until-marker, build segment, repeat, push back), generalized to handle multi-digit counts and `[]` instead of `{}`. Accepted, 100th percentile runtime, 97.38th percentile memory. Declined to give the own-words explanation himself at wrap-up this time ("You can explain it") — recurrence of the softer declined-explanation pattern first seen at LC 34 (patterns.md #69), not a full shutdown, but the second occurrence now. Explanation was written directly into the learnings file. Left one unused helper function (`repeat`, leftover from the fundamentals module's pattern) as dead code — flagged, not fixed. Also opened a broader discussion of alternative approaches (recursive descent, two-parallel-stacks) which he asked to note down for a future session rather than implement now.
- Valid Parentheses (LC #20) — 2026-07-15 — Phase 4 curriculum #1, Stack — Matching Brackets. Second and confirming instance in the same day of a course-fundamentals pattern transferring cold to a real LeetCode problem: unprompted connected this to `befittingBrackets` from the fundamentals module (push the expected closer, match against stack top, pop or fail). Clean, correct, no bugs. Own-words explanation given fully and correctly at wrap-up without prompting — this closes the open carry-forward question about whether the LC 345 transfer was a one-off; it's now a two-for-two pattern in the same session.
- Reverse Vowels of a String (LC #345, bonus) — 2026-07-15 — Off-curriculum Phase 4 (Stack) bonus, Push/Pop Reversal, first real LeetCode problem tackled after building the 5-problem stack fundamentals module (reverseSomeChars → nestingScore) in this same session. Clean, correct, no bugs, no hints. Notably: he independently recognized this was the exact same pattern as the fundamentals `reverseSomeChars` problem (push target chars on a first pass, pop them back in reverse on a second pass) without being prompted — first clean instance of a course-fundamentals pattern transferring directly to a real interview-style problem.
- Find First and Last Position of Element in Sorted Array (LC #34, bonus) — 2026-07-15 — Off-curriculum Phase 5 (Binary Search) bonus, Leftmost/Rightmost Boundary. Directly composed the `findLeftmostIndex`/`findRightMostIndex` helpers already built in the fundamentals work — no new logic needed, clean and correct on arrival. Self-disclosed "pretty easy... knew it from the video." At wrap-up, declined to answer the own-words explanation himself (why checking only `rightIndex === -1` is a complete existence check) and asked for it to be written explicitly into the notes instead — softer version of the recurring pattern (not a shutdown, just deferred the verbalization to the written record). Explanation was written directly into the learnings file. This session opened with no revisit-queue offer or mention at all — the direct conversation promised for "next session" twice now still hasn't happened.
- Binary Search (LC #704), Search Insert Position (LC #35, bonus), Sqrt(x) (LC #69, bonus) — 2026-07-14 — Opened Phase 5 (Binary Search) off-curriculum, three problems in one bundled session. **All three video-assisted (Alvin's course), disclosed honestly.** All three arrived correct and bug-free — nothing to debug. At wrap-up, declined the felt/difficulty read and the own-words explanation of why `return low` (round up, LC 35) vs `return high` (round down, LC 69) gives the correct answer on a miss. The coach gave the explanation directly and offered the LC-986-style recovery (verify with a fresh trace instead of a verbal recap) — this was also declined ("No let's just wrap up"). New data point: the trace-recovery technique that worked at LC 986 isn't universally effective; it depends on in-the-moment engagement. Logged with a shortened revisit fuse (2026-07-28) requiring the own-words explanation before coding, same protocol as LC 200/3169. This is also the fourth straight session (after LC 3169, LC 28, LC 1672) where pre-solved/bonus material arrived instead of curriculum work or the overdue revisit queue — flagged directly to him this session; he asked to defer that conversation until after this wrap-up.
- Richest Customer Wealth (LC #1672, bonus) — 2026-07-14 — Off-curriculum Phase 17 (Math & Geometry) bonus, Matrix — Row Sum Tracking. Trivial, clean, self-written solve — sum each row, `Math.max` to track the running best. No bugs, no hints needed. Session opened with the cold LC 3 revisit stated as a hard precondition (per the prior session's own recommendation) and he overrode it twice in a row — third straight session deferring the revisit queue, and the second distinct framing to fail. Flagged directly that this needs a real conversation next session, not another process change.
- Find the Index of the First Occurrence in a String (LC #28, bonus) — 2026-07-13 — Off-curriculum Phase 3 (Sliding Window) bonus, String Matching (brute force). Brought a fully correct, self-written brute-force solution to the session — no bugs, clean first paste. Correctly reasoned complexity cold (O(m·n) time, O(1) space, named `m`/`n` to the right variables) and, when asked, correctly traced the `needle.length > haystack.length` edge case (bound goes negative, loop safely never runs). KMP was introduced conceptually as the optimization (LPS array, why `i` never backtracks, O(m+n)) after one hint didn't land and he asked directly — deferred building it to a future session. Session opened with the revisit queue offered per protocol and overridden a second straight time ("Let's finish this first") — now 13 sessions deferred.
- Count Days Without Meetings (LC #3169, bonus) — 2026-07-12 — Phase 16 bonus, Intervals (merge + gap-sum). **Video-assisted solve, disclosed upfront**, but unlike LC 986 this one had real, substantive bugs in the code he brought — a syntax error (`result.push[next]`, bracket notation instead of a call, silently doing nothing), a structural bug (the push sat unconditionally after the `if` instead of in an `else`, so merged intervals got a duplicate/overlapping leftover entry pushed alongside them), and a missing `-1` in the gap-sum formula for adjacent-but-non-overlapping meetings. All three were found and fixed through real debugging over this session: the syntax error was given directly after he asked, but he then independently traced a concrete overlapping example to see the *second* bug's consequence himself (correctly predicted the corrupted `result` array and the resulting negative-gap sum), and added the `-1` fix to the gap formula independently, without being told — a genuine, non-trivial correctness catch. At wrap-up, declined the ownership-check questions outright ("you can't explain it by yourself? Don't ask me these questions today") and ended the session. Unlike LC 986, there was no attempt to recover the explanation before ending — logged with a short revisit fuse similar to LC 200's unresolved-ownership treatment, despite the real debugging work shown mid-session.
- Interval List Intersections (LC #986, bonus) — 2026-07-11 — Phase 16 bonus, Two Pointers (two-list variant). **Video-assisted solve, disclosed honestly upfront** (same as LC 200). Code was correct on the first paste (standard two-pointer overlap-check-and-advance-smaller-end solution), with one already-self-corrected `const`/loop-bound typo caught before submission. At wrap-up, initially declined the own-words explanation and asked the coach to answer it — but when pushed with one specific, narrow question ("why advance the pointer with the smaller end?") and then a concrete trace (`[0,2]`/`[1,5]` → `[5,10]`/`[1,5]`), self-caught a boolean-evaluation slip (stated "e1<e2" as the reason for a step where it was actually false) and then correctly rebuilt the "exhausted interval" reasoning from scratch, unprompted. Meaningfully different outcome from LC 200: initial disengagement, but ownership recovered within the same wrap-up via targeted questioning rather than requiring a separate cold redo to prove it. Logged with a video-assisted flag and a shortened revisit fuse, but not treated as unresolved the way LC 200 was.
- Meeting Rooms (LC #252) — 2026-07-10 — Phase 16, third Intervals problem, first curriculum entry in the phase (LC 57/56 were done first despite being later in phase order). Clean, fast solve — wrote a correct sort-then-single-pass solution on the very first attempt, one recurring self-caught bug (`const current` reassigned later in the loop — same shape logged multiple times before, e.g. LC 57). Correctly reasoned complexity (O(n log n) time from sort, O(1) space) and correctly traced the touching-interval edge case (`[5,10]`/`[10,15]` → allowed) unprompted. Continued the now-established "is there a better way to write this" code-cleanliness habit (third session in a row): spotted, via a guided trace, that the `if (intervals.length <= 1) return true` guard was dead code (loop already handles length 0/1 by never executing), then independently proposed and wrote a further simplification removing the `current`/`next` variables entirely in favor of direct `intervals[i]`/`intervals[i-1]` comparisons. At wrap-up, declined to answer the standard reflection questions himself ("you can read the chat and answer the questions") — different in character from the LC 200 video-solve disengagement, since he was actively deriving and typing every line throughout this session; read as low-effort-at-the-end on an easy/quick problem rather than a comprehension gap. Revisit queue deferred a third straight time, this time with an explicit "next week" commitment.
- Merge Intervals (LC #56) — 2026-07-07 — Phase 16, second Intervals problem, same day as LC 57. Opened incompletely (assumed left-to-right scanning alone works, without checking sort order) but corrected the moment asked whether the array was guaranteed sorted. Strong transfer session: independently re-derived the loop-invariant trick from LC 57 in a new form (recognizing `a<=d` is always true once sorted, so only `b>=c` matters, and `min(a,c)` always equals `a`), and self-connected a missing-final-push bug directly to the identical bug shape from Insert Interval without being told first. Caught a second dead-branch redundancy (`else if` whose condition is the negation of the preceding `if`) using the same instinct. Energy dropped during the post-acceptance code-cleanliness discussion (terser answers, asked to just be shown the refactor), but closed with a genuine unprompted metacognitive question — "why couldn't I think of this myself" — about noticing two variables tracking the same fact. Accepted 172/172, 6.82nd/36.27th percentile (flagged as an open question to dig into next time — surprisingly low for O(n log n)). Revisit queue explicitly declined a second straight session ("no redos now").
- Insert Interval (LC #57) — 2026-07-07 — Phase 16 (first Intervals problem, new pattern). Opened as a total blank ("I don't even know how to solve this"). Through Socratic questioning independently derived the three-case classification (interval entirely before / overlapping / entirely after `newInterval`) and the merge formula (`min` of starts, `max` of ends) before writing any code. Coding surfaced real bugs, all caught via tracing: `const` reassignment on a variable he later needed to mutate (recurring habit), a `Math.min`/`Math.max` mixup in the merge line, and — the one mechanism that needed to be given directly after two stuck "idk" responses — a missing one-time push of `newInterval` when no interval ever falls strictly after it. Then, unprompted, asked "is there a better way to write this" purely about code cleanliness (not complexity), was walked through the cleaner three-while-loop refactor, and correctly reasoned through a loop invariant himself (why the merge loop only needs to check one half of the overlap condition, since the first loop already guarantees the other half). Accepted, 59th/55th percentile. Two full solutions logged (flag-based single-pass, and three-while-loop refactor).
- Max Area of Island (LC #695) — 2026-07-06 — Phase 13, DFS — Flood Fill (accumulate area). **This was the LC 200 ownership test, and it passed clean.** Attempted fully cold with zero mention of Number of Islands beforehand — independently reproduced the entire self-guarding recursion shape (bounds → water → visited → mark → recurse) from memory, adapted correctly for area-accumulation instead of a boolean. Only small bugs: `const` reassignment and a missing water-check guard, both self-corrected in one trace each; plus the all-water edge case (`maxArea` should start at `0`, not `-Infinity`). Also built the mutate-in-place alternative (flip counted cells to `0`, no Set) after agreeing to see it — jumped from 22nd/48th percentile to 77th/87th.
- Evaluate Division (LC #399) — 2026-07-05 — Phase 13 bonus, Weighted Graph DFS. Fifth problem of the day, genuinely started from "I have no idea" — full from-scratch Socratic build: recognizing division equations as a weighted directed graph (reciprocal edge per equation), DFS with a running product, and a proper `-1` failure sentinel with return-immediately-on-success (the hardest new piece — several rounds of tracing before the "multiply across all branches" bug was fixed). Correctly self-derived the graph model and the visited-set need; needed direct answers on the final DFS combining logic and on complexity reasoning (both after repeated "idk"/fatigue signals). Learned what Union-Find is (conceptual only, not yet implemented) as a preview for later Graphs-phase problems.
- Clone Graph (LC #133) — 2026-07-05 — Phase 13, Graph Traversal — Clone-and-Reuse via Map. Fourth problem of the day, energy clearly recovered from LC 997 — clean, mostly self-driven build with only small mechanical nudges (constructor `new` keyword, variable shadowing, ordering fix). Wrote and compared THREE full solutions (recursive DFS, iterative BFS, iterative DFS with explicit stack) — the BFS/iterative-DFS pair is now the clean proof-by-example that DFS vs BFS is about which end of a pending-work list you pull from, not about recursion vs loops. Built a three-way comparison visualiser with side-by-side original/clone graph rendering.
- Find the Town Judge (LC #997) — 2026-07-05 — Phase 13 bonus, Graph — In-degree/Out-degree counting. Third problem of the day. Brought an already-attempted (wrong) solution that checked only out-degree; debugged it into the full correct condition through tracing, with several direct answers given late in the session as energy ran low (3am) — `??` nullish-coalescing recall gap, and a couple of "just tell me" moments on complexity. One clean self-correction: caught his own `outDegreeCount[b]++` typo (should be `[a]`) via a targeted trace, no hint needed beyond the trace itself.
- Surrounded Regions (LC #130) — 2026-07-05 — Phase 13, Graph DFS — Region Decision (walk full region via flood fill, decide safe/captured after, flip in a second pass). Self-derived almost entirely through Socratic tracing — strong contrast to LC 200 earlier the same day. Only two direct answers given (bounds-check direction fix, `r`/`c` typo), both after he explicitly asked. Also built and iterated a comparison visualiser against the border-first flood variant (Solution B); flagged himself that Solution B needs more understanding — open item.
- Number of Islands (LC #200) — 2026-07-05 — Phase 13 (first graphs problem), Graph DFS — Flood Fill on grid + visited Set. **Video-assisted solve** (learned approach from a video, flagged this himself — honest). Declined to explain solution in own words at wrap-up → recognized, not yet owned; short-fuse cold redo scheduled 2026-07-19. Key conceptual moment: believed 4 sequential recursive calls = BFS ("visiting all surrounding nodes"); corrected via call-stack trace table — first call runs to completion before second starts. Stack → DFS, queue → BFS.
- Binary Tree Preorder/Inorder/Postorder Traversal (LC #144, #94, #145) — 2026-07-01 — Phase 9 bonus (self-solved, brought pre-written solutions to session for wrap-up), DFS — Traversal Order, all three 100th percentile runtime, clean solves
- Minimum Depth of Binary Tree (LC #111) — 2026-06-29 — Phase 9 bonus, DFS null-child-aware minimum, 43rd percentile, self-diagnosed the Math.min null-child bug
- Balanced Binary Tree (LC #110) — 2026-06-29 — Phase 9, DFS + sentinel (-1) for early exit, 83rd percentile, needed sentinel concept explained; independently attempted closure variant after
- Diameter of Binary Tree (LC #543) — 2026-06-28 — Phase 9, DFS post-order + closure variable, 87th percentile, struggled with two-output distinction (return vs best update), unlocked via scaffold + visualizer
- Count Complete Tree Nodes (LC #222) — 2026-06-28 — Phase 9 bonus, O(n)×3 cold + O(log²n) guided, 100th percentile optimal
- Path Sum (LC #112) — 2026-06-28 — Phase 9 bonus, iterative DFS cold + recursive guided, 100th percentile recursive
- Symmetric Tree (LC #101) — 2026-06-27 — Phase 9 bonus, DFS recursive, 100th percentile, mirror pairing concept explained from scratch
- Invert Binary Tree (LC #226) — 2026-06-27 — Phase 9, DFS pre-order, 100th percentile, clean solve, traversal order discussion
- Same Tree (LC #100) — 2026-06-27 — Phase 9, DFS recursive lockstep, 100th percentile, base case reasoning unlocked via concrete trace
- Maximum Depth of Binary Tree (LC #104) — 2026-06-27 — Phase 9, DFS (iterative pairs + recursive), 100th percentile recursive, clean transfer from fundamentals
- Substring with Concatenation of All Words (LC #30) — 2026-06-25 — Phase 3 bonus, Sliding Window Fixed Size + word frequency map comparison, 41st percentile, guided solve — anagram instinct misapplied at start
- Minimum Size Subarray Sum (LC #209) — 2026-06-24 — Phase 3 bonus, Sliding Window Variable Size (minimize), 67th percentile, struggled with "add unconditionally" structure
- Reverse Bits (LC #190) — 2026-06-16 — Phase 18, Mask & Shift (build variant — shift `result` left then OR in bit, 32-iteration `for` loop, `>>>` for the consuming shift), 63rd percentile runtime, multiple guided bugs worked through (OR-before-shift order, `while(n!==0)` terminating too early, `n >> 1` vs `n >>> 1` sign-extension risk)
- Number of 1 Bits (LC #191) — 2026-06-16 — Phase 18, Mask & Shift (`n & 1` + `n = n >>> 1`), 100th percentile runtime, one toolkit misconception corrected (no decimal→binary conversion needed) plus one self-caught bug (missing shift assignment)
- Single Number (LC #136) — 2026-06-16 — Phase 18 (bonus pull-forward), XOR self-cancellation, 72nd percentile, clean first-attempt solve, no hints needed
- Longest Consecutive Sequence (LC #128) — 2026-06-15 — Phase 1 #9, Hash Set + sequence-start filter, 84th percentile (unclean) / clean rewrite
- Happy Number (LC #202) — 2026-06-15 — Phase 17 bonus, cycle detection (Hash Set) + digit extraction via `% 10` / `Math.floor`, 100th percentile
- Contains Duplicate II (LC #219) — 2026-06-15 — Hashmap sprint bonus, sliding window (Set) + HashMap (last seen index), 82nd percentile
- Word Pattern (LC #290) — 2026-06-15 — Hashmap sprint bonus, bijection (two maps), 100th percentile
- Isomorphic Strings (LC #205) — 2026-06-15 — Hashmap sprint bonus, bijection (two maps), 90th percentile
- Ransom Note (LC #383) — 2026-06-14 — Hashmap sprint bonus, frequency counting (increment/decrement), clean first-attempt solve
- Longest Substring Without Repeating Characters (LC #3) — 2026-06-14 — 27th percentile, Sliding Window (HashMap + left pointer), Phase 3
- Group Anagrams (LC #49) — 2026-06-14 — 45th percentile, sorted string as HashMap key, Phase 1 cleanup
- Valid Anagram (LC #242) — 2026-06-14 — two submissions (two-map + one-map), Phase 1 cleanup
- Best Time to Buy and Sell Stock (LC #121) — 2026-06-13 — 80th percentile, Running Min / Single Pass (Phase 3 start)
- Trapping Rain Water (LC #42) — 2026-06-09 — 100th percentile (two-pointer), Phase 2 final
- Contains Duplicate (LC #217) — 2026-06-07 — 65th percentile, Hash Set (Phase 1 cleanup)
- Pow(x, n) (LC #50) — 2026-06-07 — 100th percentile, Exponentiation by Squaring (detour into Phase 17)
- Majority Element (LC #169) — 2026-06-07 — 100th percentile, Boyer-Moore Voting (bonus problem, not NeetCode 150)
- Remove Duplicates from Sorted Array II (LC #80) — 2026-06-06 — 46th percentile, read/write with look-back
- Remove Duplicates from Sorted Array (LC #26) — 2026-06-06 — 100th percentile, read/write pointer
- Remove Element (LC #27) — 2026-06-06 — 100th percentile, swap + read/write pointer approaches
- Merge Sorted Array (LC #88) — 2026-06-05 — 100th percentile, two pointers fill backwards
- 3Sum (LC #15) — 2026-06-04 — 67th percentile, two pointers + k-sum reduction
- Container With Most Water (LC #11) — 2026-06-04
- Squares of a Sorted Array (LC #977) — 2026-06-04
- Two Sum II (LC #167) — 2026-06-03 — 100th percentile
- Valid Palindrome (LC #125) — 2026-06-03 — 5th percentile (charCodeAt rewrite pending)

---

## Concepts Mastered
- [x] Hash map / complement lookup
- [x] Two pointers — converging (sorted guarantee)
- [x] Two pointers — both ends, fill backwards
- [x] Two pointers — greedy move (Container With Most Water)
- [x] Two pointers inside a loop (k-sum reduction)
- [x] Right-to-left in-place update (Pascal's Triangle II)
- [x] Recursion: base case + recursive case + guaranteed progress
- [x] Memoization (top-down DP) — cache parameter, check, store
- [x] Cache correctness: `if(n in cache)` not `if(cache[n])`
- [ ] Two-pointer recursion with early termination
- [ ] Bottom-up DP (tabulation)
- [ ] Sliding Window (in progress — LC 121, LC 3 done)
- [ ] Stack / Monotonic Stack

---

## Phase 1 — Arrays & Hashing
3 problems need a redo (done before repo was set up, no solution on file):
- Contains Duplicate (LC #217)
- Valid Anagram (LC #242)
- Group Anagrams (LC #49)

These will be revisited when we loop back to Phase 1 cleanup.
