# Session: Two Sum (LC 1) — 2026-06-03

## What He Attempted

Brute force first — nested loop checking all pairs. Correct on first attempt.

Then hash map solution: store complement→index as you iterate, check if current number exists as a complement before storing. Also correct, with one typo caught during trace (`num[i]` → `nums[i]`).

## Where He Got Stuck

Not stuck on the algorithm. One moment of confusion: the plan coming in was to practice two pointers, but LC 1 is unsorted — two pointers don't apply. He correctly identified this himself ("the array is not sorted?") and asked why. Added as a carry-forward question rather than derailing the session.

## Mistakes Made

- Typo: `complements.get(num[i])` instead of `complements.get(nums[i])` — caught himself during trace

## Key Insight

Time-space trade-off: brute force is O(n²) time / O(1) space. Hash map is O(n) time / O(n) space. You're buying speed with memory.

## Complexity Reached

Time: O(n) — Space: O(n)

## Coach Notes for Next Session

- Two Sum II (LC 167) is the natural next step — same problem on a sorted array, two pointers apply, O(1) space
- Probe the carry-forward question: why can't two pointers work on an unsorted array? What property is required?
- He's comfortable with hash maps now — don't re-explain Map.has/get/set
