# Session: Find the Index of the First Occurrence in a String (LC 28) — 2026-07-13

## What He Attempted

Brought a fully-written, working brute-force solution:

```js
var strStr = function(haystack, needle) {
    if(needle.length === 0) return 0;
    for(let i = 0; i < haystack.length + 1 - needle.length; i++) {
        for(let j = 0; j < needle.length; j++) {
            if(haystack[i + j] !== needle[j]) break;
            if(j === needle.length -1) return i;
        }
    }
    return -1
};
```

## Where He Got Stuck

Not on the brute force itself — that was already correct and self-written. Got stuck when asked to identify what's wasteful about the O(m·n) approach and think toward an optimization (KMP). Asked for the hint, then asked to be told directly.

## Mistakes Made

None in the code itself. No bugs to debug this session — the solution was correct on the first paste.

## Key Insight

Brute force: for each starting index `i` in `haystack`, compare `needle` character by character; `break` on mismatch, return `i` on full match. Correctly reasoned complexity cold: O(m·n) time (m = haystack length, n = needle length), O(1) space. Correctly traced the `needle.length > haystack.length` edge case unprompted-once-asked — the loop bound goes negative, so the `for` loop never executes and it falls through to `return -1` safely.

## Complexity Reached

Time: O(m·n) — self-reasoned cleanly, correctly named which variable was which.
Space: O(1)

## Coach Notes for Next Session

- **Session-start protocol override, second in a row.** Same as LC 3169 (2026-07-12): offered the overdue revisit queue (LC 3, oldest overdue since 2026-06-18) per the explicit "no longer optional, state it and proceed" instruction from the last handoff, and he directly redirected to this problem instead ("Let's finish this first"). The revisit queue is now **thirteen** straight sessions deferred. This needs a harder line next session — see the top of `safwaan/progress.md`.
- KMP was introduced conceptually (LPS array, why `i` in haystack never backtracks, O(m+n)) after a direct "just tell me" request, following one hint that didn't land. He deferred actually building it — noted in carry-forward as an open implementation to do in a future session, likely as a return-visit to this same LC 28 problem rather than a fresh bonus entry.
- Declined the "explain in your own words" wrap-up question — just said "felt easy, straightforward brute force." Low stakes here: this was a fully self-written, untroubled solve where he actively reasoned through complexity and traced the edge case live in the session, so ownership is already well-evidenced in the transcript. Not comparable to the LC 200/3169 video-assisted ownership concerns.
