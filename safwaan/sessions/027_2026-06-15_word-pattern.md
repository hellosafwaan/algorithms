# Session: Word Pattern — 2026-06-15

## What He Attempted

Two-map solution straight from the start — no prompting needed. Split `s` into words with `s.split(" ")`, iterated in parallel with `pattern[i]`. Used `patternMap` (char → word) and `sMap` (word → char), with check-then-set on both.

First submission had two bugs in the `.set()` calls: `patternMap.set(sWord)` and `sMap.set(pChar)` — wrong key used in both, and no value argument. Also the second `if` check compared against `sWord` instead of `pChar`.

## Where He Got Stuck

`.set()` argument order. Asked "what are you actually storing?" — fixed all three issues immediately on his own.

## Mistakes Made

1. `.set()` argument order — used the wrong variable as key in both maps, and omitted the value. One question, self-corrected.
2. Missing `n !== s.length` guard — self-caught before submitting, added unprompted.

## Key Insight

Two-map bijection from LC 205 transferred directly. The only structural difference: `s` is space-separated words, not individual characters, so split it first. Pattern char maps to word; word maps back to pattern char. Length mismatch = structural mismatch, catch it at the top.

## Complexity Reached

Time: O(n) — one pass, O(1) map ops per step  
Space: O(n) — two maps, each bounded by n entries

## Coach Notes for Next Session

- Bijection pattern is transferring cleanly. `.set(key, value)` argument order is the one rough edge — not a conceptual gap, just sloppiness under implementation pressure.
- "Why one map fails" — answered cold, perfectly, before attempting. His own counterexample: "A maps to dog, B maps to dog — one-directional true, other direction false." That's exactly it.
- Next: Happy Number (LC 202). New territory — fast/slow pointer or hash set for cycle detection. Let him attempt cold.
- LC 3 cold redo still scheduled for 2026-06-18 (Wednesday).
