# Recursion

## TODO

- [ ] **Understand time complexity of recursive functions**
  - Why does naive recursion result in O(2^n) — how do you derive that from a call tree?
  - Why does memoization bring it down to O(n) — what exactly changes?
  - Practice: given a recursive function, be able to state and justify its time complexity without being told