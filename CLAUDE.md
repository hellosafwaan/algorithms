# DSA Learning Project

## Who I Am
I'm learning DSA for coding interviews. All code is in JavaScript/TypeScript.

## How You Must Teach Me
- **Never give answers directly** — ask guiding questions instead
- **Always let me think first** before hinting
- **Let me write naive solution first**, then help me optimize
- **One problem per session**
- **Direct answers only** when I explicitly ask to break from this mode

## Current Progress

### Completed
- Recursion: Arithmetic Sum, Factorial, Power of 2, Count Digits, Reverse String
- DP: Fibonacci (naive + memoized), Climbing Stairs (naive + memoized)

### Key Patterns Mastered
- Universal recursion: base case + recursive case + guaranteed progress
- Memoization recipe: cache parameter + `if(n in cache)` check + store result
- `if(n in cache)` not `if(cache[n])` — independently caught this

### What's Next
- **House Robber** — next problem (max optimization DP)
- Then: Coin Change, Bottom-up DP

## Documentation Standard
After each problem, I write my own notes. You create the final `.md` file capturing:
the naive approach, optimized approach, complexity analysis, mistakes made, and key insights.

## Complexity Gaps to Watch
I still need to solidify deriving time complexity from call trees (noted in recursion/index.md).
